# if/cnh Discord Bot

## planned updates:
- if can have, anonymous report of what channels get used the most pls?
- add existsSync check so bot doesnt crash if its started somewhere without a report json